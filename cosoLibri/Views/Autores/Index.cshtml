@model IEnumerable<cosoLibri.Models.Autore>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cognome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nazione)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cognome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nazione)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodiceAutore">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodiceAutore">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodiceAutore">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
